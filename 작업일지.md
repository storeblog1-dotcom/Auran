# 작업일지

## 2026-07-27 (Asia/Seoul)

### 21:00 전후

- 이미지 업로드 정책을 확인하고 요구사항을 정리함.
- 신규 업로드 파일 최대 용량을 `10MB`에서 `1MB`로 변경함.
- 피드 및 게시물 이미지가 최대 `400KB`가 되도록 JPEG 품질과 해상도를 단계적으로 조정하도록 구현함.
- 최대 이미지 변은 `1080px`로 유지하고, 비율은 유지하도록 처리함.
- 변환 실패 시 원본을 유지하는 Supabase 일괄 변환 스크립트를 추가함.

### 21:19-21:22

- GitHub Actions를 통해 Cloud Run 백엔드를 배포함.
- 배포 리비전 `instagram-backend-00020-mv5`가 트래픽 `100%`를 수신하도록 확인함.
- 서비스 URL: `https://instagram-backend-110122614099.asia-northeast3.run.app`

### 21:24

- Supabase 이미지 변환 workflow를 처음 실행했으나 Python import 경로 오류로 실패함.
- workflow에 `PYTHONPATH`를 추가하고 main에 수정사항을 반영함.

### 21:27-21:28

- Supabase 버킷의 이미지 `13개`를 일괄 변환함.
- 성공 `13개`, 실패 `0개`.
- 변환 후 최대 파일 크기는 약 `194KB`.
- 원본 파일은 별도 백업하지 않고 변환 이미지로 교체함.

### 21:30 전후

- 변환된 이미지 `13개`를 `E:\download`에 다운로드함.
- 기존 파일은 덮어쓰지 않음.

### 21:45 전후

- Expo 개발 서버를 LAN 모드로 재시작함.
- `8081` 포트에서 Metro Bundler가 실행 중임을 확인함.
- Expo Go 접속 주소: `exp://172.30.1.82:8081`
- 휴대폰과 개발 PC가 같은 Wi-Fi에 있어야 함.

### 보안 확인

- GitHub 인증 토큰을 코드, 로그, 환경설정 파일에 추가하지 않음.
- 배포 및 Supabase 변환에는 GitHub Secrets를 사용함.

## 2026-07-28 (Asia/Seoul)

### 닉네임 표시 및 데이터 보정

- 사용자에게 보이는 아이디 표기를 닉네임 우선으로 전환함: 피드, 게시물·댓글, 프로필, 검색, 알림, DM, 스토리, 커뮤니티, 관리자 화면.
- 닉네임이 비어 있는 기존 계정에는 생성일 및 ID 순으로 `아우라00001`부터 자동 닉네임을 저장하는 Alembic 마이그레이션을 추가함.
- 자동 닉네임은 이미 사용 중인 번호를 건너뛰고 다음 번호를 사용하며, 10만 명 이후에도 자릿수를 늘려 계속 생성하도록 구현함.
- 회원가입 및 프로필 수정 화면에 닉네임 중복 확인을 추가하고 서버에서도 대소문자 구분 없이 중복을 차단함.

### 댓글 및 답글 UX

- 댓글의 답글·수정·삭제 동작을 별도 액션 행으로 분리해 좁은 화면에서도 겹치지 않도록 수정함.
- 답글 상태 문구와 입력 안내를 아이디 대신 닉네임으로 표시하도록 통일함.
- 댓글 입력칸에 자동으로 들어가던 `@아이디`를 제거함.
- 답글 대상 사용자 ID를 별도로 전달해, 입력 내용과 관계없이 멘션 알림이 발송되도록 구현함.
- 게시물 상세 화면에 `작성자에게 답글` 칩을 추가해 게시물 작성자에게 바로 답글을 시작할 수 있게 함.

### 검증 및 배포

- 백엔드 `python -m compileall app`, 프런트엔드 `npx tsc --noEmit` 검증 통과.
- PR #10: 닉네임 기능 병합 및 Cloud Run 배포 성공. Supabase DB 마이그레이션 성공.
- PR #11, #12: 프런트엔드 댓글·답글 표시 개선 병합.
- PR #13: 답글 멘션 알림 및 작성자 답글 기능 병합. Cloud Run 배포 및 Supabase DB 마이그레이션 성공.
- Expo Metro Bundler를 LAN 모드로 재시작함. Expo Go 주소: `exp://172.30.1.62:8081`.

### 작성자 답글 UI 조정

- 게시물 상세 화면의 `작성자에게 답글` 기능을 프로필 헤더에서 댓글 목록 하단으로 이동함.
- 게시물 작성자 본인에게도 버튼이 표시되도록 조정함.
- 버튼을 누르면 작성자를 답글 및 멘션 알림 대상으로 설정하고, 댓글 입력창에는 내부 아이디를 자동 입력하지 않음.
- 프런트엔드 TypeScript 검증 통과 후 PR #15로 `main`에 병합함.

## 2026-07-29 (Asia/Seoul)

### 승인된 구현 브리프

- 게시물과 댓글·답글의 작성·수정·삭제 시점별 텍스트 상태 및 당시 IP를 관리자 전용 불변 버전으로 3년간 보존하기로 확정함.
- 미디어 원본은 버전마다 복제하지 않고 기존 저장소 URL, 미디어 종류 및 표시 순서만 보존해 데이터 증가를 제한하기로 함.
- 삭제된 콘텐츠는 일반 사용자에게 즉시 노출되지 않게 하되, 관리자는 활동 로그와 사용자별 콘텐츠 목록에서 계속 열람할 수 있게 함.
- 기존 활성 게시물과 댓글은 배포 시점의 상태를 기준 버전으로 생성하고, 정책 적용 전에 이미 삭제된 자료는 복구 대상에서 제외함.
- 수사기관 등의 적법한 요청이 있는 자료는 법적 보존 상태로 설정해 자동 파기에서 제외하며, 일반 기록은 생성 시점부터 3년이 지나면 자동 파기하기로 함.
- 관리자의 강제 삭제·열람·법적 보존 조작은 일반 사용자 활동 로그에 추가하지 않기로 함.

### 백엔드 및 데이터 보존 구현

- `post_revisions`, `comment_revisions` 테이블과 감사 로그의 `revision_id`, `retention_until`, `legal_hold` 필드를 추가함.
- 게시물과 댓글·답글의 작성·수정·삭제 흐름에 전체 상태 버전 생성과 클라이언트 IP 기록을 연결함.
- 게시물 또는 상위 댓글 삭제 시 함께 삭제되는 댓글·답글도 삭제 직전에 모두 보존하도록 처리함.
- 기존 게시물·댓글·답글을 기준 버전으로 일괄 생성하는 Alembic 백필 마이그레이션을 추가함.
- 삭제된 게시물·댓글도 관리자 사용자별 콘텐츠 목록에 고유번호, 콘텐츠 종류, 게시판 종류 및 내용과 함께 표시하도록 조회를 확장함.
- 관리자 강제 삭제도 보존 버전은 생성하되 관리자 활동 이벤트는 만들지 않도록 처리함.

### 관리자 활동 로그 및 팝업

- 새 감사 로그가 해당 시점의 콘텐츠 버전을 직접 참조하도록 연결함.
- 게시물·댓글 활동 로그를 누르면 이벤트 당시 제목·본문·댓글·답글·미디어 참조·IP·보존기한을 관리자 전용 팝업으로 표시하도록 구현함.
- 삭제된 게시물과 댓글도 동일한 팝업에서 열람 가능하도록 함.
- 팝업에서 법적 보존을 설정하거나 해제할 수 있도록 하고, 게시물 법적 보존 시 연결된 댓글·답글 및 감사 로그에도 함께 적용함.

### 자동 파기 및 운영방침

- 보존기한이 지난 게시물·댓글 버전과 감사 로그를 매일 정리하는 GitHub Actions workflow를 추가함.
- 법적 보존 상태인 기록은 자동 파기에서 제외함.
- `운영방침.md`에 시점별 버전 보존, 미디어 비복제, 3년 보존기한, 자동 파기 및 법적 보존 예외를 반영함.

### 검증 및 배포

- 백엔드 단위 테스트 `4개`, Python 컴파일, Alembic head 및 OpenAPI 경로 검증을 통과함.
- 프런트엔드 `npx tsc --noEmit`과 Expo 웹 번들 생성을 통과함.
- PR #25로 전체 기능을 `main`에 병합함.
- 첫 배포에서 Alembic revision ID가 DB의 32자 제한을 초과해 마이그레이션이 트랜잭션 롤백됨.
- revision ID를 단축하고 모든 마이그레이션 ID 길이를 검사하는 회귀 테스트를 추가해 PR #26으로 병합함.
- 최종 배포에서 Alembic 마이그레이션과 기존 콘텐츠 백필, Cloud Run 배포가 모두 성공함.
- 운영 헬스 응답 `status: ok`와 관리자 보존 상세 OpenAPI 경로 반영을 확인함.
- 최종 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30381098271`

### 추가 승인 브리프: 7일 탈퇴 대기와 개인정보 365일 제한

- 탈퇴를 신청하면 바로 로그아웃되고 일반 서비스는 이용할 수 없게 한다.
- 신청 후 7일 동안 다시 로그인하면 `탈퇴 대기 중` 안내만 보여주고, 본인확인 후 탈퇴를 취소하면 계정을 즉시 정상 복구한다.
- 7일이 지나면 탈퇴가 최종 확정되어 로그인과 탈퇴 취소가 불가능해진다.
- 최종 탈퇴 후 개인정보는 최대 365일만 보관한다.
- 365일이 지나면 아이디·닉네임·이름·이메일·프로필·인증 정보·IP를 삭제하거나 알아볼 수 없게 바꾼다.
- 게시물과 댓글·답글은 유지하되 작성자는 `탈퇴한 사용자`로 표시한다.
- 수사기관 등의 적법한 요청으로 법적 보존이 설정된 자료만 365일 이후에도 예외적으로 보관한다.

### 구현 내용

- 탈퇴 계정에 신청 시각, 취소 가능 기한, 최종 탈퇴 시각, 개인정보 파기 기한 및 법적 보존 상태를 저장하도록 데이터 구조를 변경함.
- 기존 탈퇴 계정은 이미 최종 탈퇴된 상태로 이관하고, 기존 3년 보존기한을 최대 365일로 단축하는 마이그레이션을 추가함.
- 일반 로그인과 Google 재인증에서 탈퇴 대기 계정에 일반 서비스용 토큰 대신 탈퇴 취소 전용 토큰만 발급하도록 구현함.
- 탈퇴 취소 전용 토큰은 일반 API에서 사용할 수 없으며, 7일 이내 탈퇴 취소에만 사용할 수 있도록 제한함.
- 프로필 편집 화면에 비밀번호 재입력, `탈퇴` 직접 입력 및 최종 확인을 거치는 탈퇴 신청 UI를 추가함.
- 탈퇴 대기 계정으로 로그인하면 남은 시간을 표시하고 계정을 복구할 수 있는 전용 화면을 추가함.
- 7일 만료 확인과 365일 개인정보 익명화·IP 및 감사기록 파기를 매시간 실행하도록 자동 정리 workflow를 변경함.
- 다른 사용자나 게시물이 참조하지 않는 탈퇴 계정 프로필 이미지만 Supabase Storage에서 안전하게 삭제하도록 처리함.
- 법적 보존 상태인 탈퇴 계정과 연결 자료는 자동 파기에서 제외하도록 구현함.
- 관리자 사용자 목록에서 `탈퇴 대기`, `최종 탈퇴`, `개인정보 파기` 상태와 취소·파기 예정 시각을 구분해 확인할 수 있도록 보완함.
- 백엔드 단위 테스트 5건, OpenAPI 경로 검사, Alembic 최신 마이그레이션 검사, TypeScript 검사 및 Expo 웹 번들 생성을 통과함.
- PR #28을 `main`에 병합하고 GitHub Actions에서 Supabase Alembic 마이그레이션과 Cloud Run 배포가 모두 성공함.
- 운영 서버 헬스 응답 `status: ok`와 탈퇴 신청·취소·법적 보존 API 경로 반영을 확인함.
- 백엔드 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30385027278`
- 프런트엔드는 Expo Metro Bundler의 Android 번들 생성과 `8081` 포트 응답을 확인함. Expo Go LAN 주소: `exp://172.30.1.62:8081`

### 탈퇴 신청 양식 표시 보완

- 프로필 편집 하단의 `계정 탈퇴 신청` 버튼을 눌러도 양식이 화면 아래에서 펼쳐져 반응이 없는 것처럼 보이는 원인을 확인함.
- 버튼을 누르면 기존 탈퇴 양식을 펼치고 해당 위치로 자동 스크롤하도록 수정함.
- 버튼을 다시 누르면 양식이 접히며, 기존 비밀번호 확인·`탈퇴` 입력·최종 확인 절차는 그대로 유지함.

### 휴대폰 사진 게시물 작성 400 오류 수정

- 원본 이미지 업로드 제한이 1MB여서 일반적인 휴대폰 사진이 게시물 저장 전에 HTTP 400으로 거부되는 원인을 확인함.
- 원본 이미지 허용 크기를 15MB로 조정하고, 저장 파일은 기존과 동일하게 최대 1080px·약 400KB로 압축하도록 유지함.
- 게시물 작성 화면이 이미지의 실제 파일명과 MIME 형식을 전달하도록 보완함.
- 이미지 업로드 실패와 게시물 저장 실패를 구분하고, 서버의 구체적인 오류 안내를 사용자에게 표시하도록 수정함.
- 1MB를 넘는 원본의 정상 처리와 15MB 초과 원본의 명확한 거부를 검사하는 백엔드 회귀 테스트를 추가함.
- 백엔드 단위 테스트 7건, Python 컴파일, TypeScript 검사 및 Expo 웹·Android 번들 생성을 통과함.
- PR #33을 `main`에 병합하고 Supabase Alembic 마이그레이션과 Cloud Run 배포가 성공함.
- 운영 서버 헬스 응답 `status: ok`와 Expo 개발 서버의 Android 번들 응답을 확인함.
- 백엔드 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30388691870`

### 관리자 활동 로그 사용자별 통합 및 연결 상세 보기

- 활동 로그 첫 화면을 활동 건별 반복 목록에서 사용자별 한 줄 목록으로 변경함.
- 사용자를 최근 활동순으로 정렬하고 아이디·닉네임, 최근 활동 시각, 활동 건수와 가입·탈퇴 상태를 표시함.
- 사용자 행을 누르면 해당 사용자의 가입·탈퇴 IP 기록과 이미지 없는 게시물·댓글·대댓글 목록을 펼치도록 구현함.
- 현재 게시물 행을 누르면 기존 게시물 상세 형식으로 전체 게시물과 댓글·대댓글을 표시하고, 댓글·대댓글 행은 연결된 원 게시물을 열도록 구현함.
- 게시판 게시물의 상세 화면에는 관리자에게 게시판 종류와 제목을 표시하고, 이미지가 없는 경우 불필요한 이미지 빈 영역을 표시하지 않도록 함.
- 삭제된 게시물·댓글은 보존 버전 팝업으로 연결해 당시 내용, IP, 원 게시물과 댓글·대댓글 상태를 확인할 수 있도록 함.
- 콘텐츠별 변경 이력을 필요할 때만 불러오며, 각 버전을 누르면 수정 시점의 게시물 및 당시 존재했던 댓글·대댓글 상태를 재구성해 표시하도록 함.
- 사용자 콘텐츠와 변경 이력을 화면에서 캐시하고, 사용자 목록은 페이지 단위로 추가 로딩해 조회 시간을 줄임.
- 모든 신규 조회 API는 기존 관리자 권한 검사를 그대로 적용하며 일반 사용자 화면의 동작은 변경하지 않음.
- 백엔드 단위 테스트 9건, Python 컴파일, TypeScript 검사와 Expo 웹·Android 번들 생성을 통과함.
- PR #35를 `main`에 병합하고 Supabase Alembic 마이그레이션과 Cloud Run 배포가 성공함.
- 운영 OpenAPI에서 사용자별 활동·콘텐츠 이력·보존 상세 경로 반영을 확인하고, 비로그인 요청이 HTTP 401로 차단되는 것을 검증함.
- 운영 서버 헬스 응답 `status: ok`와 Expo Android 번들 HTTP 200 응답을 확인함.
- 백엔드 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30390947734`

### 관리자 콘텐츠 IP 표시 누락 보완

- 게시물·댓글·대댓글 목록의 각 행에 최근 작성·수정·삭제 행위 종류, 당시 IP와 시간을 표시하도록 사용자 콘텐츠 API와 관리자 화면을 보완함.
- 현재 게시물 상세 팝업에는 관리자 전용 IP 기록 카드를 추가하고, 댓글·대댓글을 통해 연 원 게시물에는 선택한 댓글의 고유번호와 최근 행위 IP가 표시되도록 연결함.
- 삭제·보존 콘텐츠 팝업에 작성·수정·삭제 전체 버전별 IP 이력을 표시하고, 각 버전을 눌러 해당 시점 상세로 전환할 수 있도록 구현함.
- IP 기록 기능 도입 전에 생성된 콘텐츠나 IP가 없는 기록은 `기록 없음`으로 명확히 표시함.
- 일반 사용자 화면에서는 관리자 IP 카드가 렌더링되지 않도록 관리자 전용 속성으로 분리함.
- 백엔드 단위 테스트 10건, Python 컴파일, TypeScript 검사와 Expo 웹·Android 번들 생성을 통과함.
- PR #37을 `main`에 병합하고 Supabase Alembic 마이그레이션과 Cloud Run 배포가 성공함.
- 운영 OpenAPI에서 사용자 콘텐츠·변경 이력·보존 상세 경로 반영을 확인하고 비로그인 접근이 HTTP 401로 차단되는 것을 검증함.
- 운영 서버 헬스 응답 `status: ok`와 Expo Android 번들 HTTP 200 응답을 확인함.
- 백엔드 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30392524860`

### 상세 게시물 원본보기 이미지 1MB 최적화

- 원본 업로드 최대 용량은 기존과 동일하게 15MB로 유지함.
- 새 피드·게시판 이미지를 업로드할 때 목록 표시용 최대 1080px·400KB 이미지와 상세 원본보기용 최대 2048px·1MB 이미지를 각각 생성하도록 변경함.
- 게시물 미디어에 상세 이미지 URL을 별도로 저장하는 필드와 기존 게시물의 상세 URL을 현재 표시 이미지 URL로 채우는 Alembic 마이그레이션을 추가함.
- 피드와 게시물 본문은 기존 400KB 표시 이미지를 사용하고, 상세 확대 팝업에서만 1MB 이하 상세 이미지를 우선 사용하도록 연결함.
- 상세 이미지가 없거나 로드에 실패하면 표시용 이미지로 자동 대체해 기존 게시물과 저장소 오류에 대응함.
- 게시물 보존 버전에는 두 URL 참조를 함께 기록하되 이미지 파일 자체는 복제하지 않도록 유지함.
- 프로필과 스토리 업로드는 기존 단일 400KB 이미지 처리 방식을 유지함.
- 백엔드 단위 테스트 11건, Python 컴파일, 단일 Alembic head, TypeScript 검사와 Expo 웹·Android 번들 생성을 통과함.
- PR #39를 `main`에 병합하고 기존 게시물 상세 URL 백필 마이그레이션과 Cloud Run 배포가 성공함.
- 운영 OpenAPI에서 게시물 미디어 생성·응답의 `detail_media_url` 필드와 이미지 업로드 경로 반영을 확인함.
- 운영 서버 헬스 응답 `status: ok`와 Expo Android 번들 HTTP 200 응답을 확인함.
- 백엔드 배포 실행: `https://github.com/storeblog1-dotcom/Auran/actions/runs/30393727482`
## 2026-07-29 신고 시스템 및 게시물 메뉴 개선

- 피드의 `…` 메뉴를 기본 알림창에서 공통 하단 시트 UI로 변경하고, 내 게시물에는 수정·공개 범위·공유·삭제, 다른 사용자의 게시물에는 프로필·팔로우·숨김·차단·신고 기능을 배치했다.
- 피드·게시판 게시물, 댓글·대댓글, 사용자 프로필을 스팸·욕설/괴롭힘·음란물·사기·불법정보·개인정보 노출·기타 사유로 신고할 수 있게 했다.
- 기타 사유는 상세 내용을 필수로 받고, 최종 확인 후 접수하며 접수 뒤 콘텐츠 숨김 또는 작성자 차단을 선택할 수 있게 했다.
- 동일 사용자의 동일 대상 중복 신고와 24시간 10건 초과 신고를 차단했다.
- 신고 시 대상 내용, 작성자, 관리자 고유번호, 게시판 종류, 작성 당시 IP 및 미디어 참조를 삭제와 분리된 증거 스냅샷으로 보존하도록 범용 신고 테이블과 마이그레이션을 추가했다.
- 기존 단순 피드 신고 기록을 새 신고 구조로 이관하고, 신고·처리 기록의 보관기간을 최대 365일로 적용했다.
- 관리자 대시보드에 대상별로 묶인 신고 관리 탭을 추가하고 신고 횟수, 사유, 신고 IP, 작성 IP, 증거 내용을 확인한 뒤 검토 중·유지·숨김·삭제·경고·정지·기각 처리할 수 있게 했다.
- 삭제된 콘텐츠도 신고 증거는 유지되며 관리자 열람·처리를 별도 감사 기록으로 남기고 처리 결과를 신고자 알림으로 전달하도록 했다.
- 일반 화면에서 운영 숨김 콘텐츠와 사용자가 직접 숨긴 콘텐츠를 제외하도록 조회 조건을 보완했다.
- 백엔드 앱 import, Alembic 단일 head, TypeScript 정적 검사를 통과하도록 검증했다.

## 2026-07-30 전체 공지 일반 공지 목록 포함 및 최신순 정렬

- 전체 공지가 신규 추가되거나 기존에 존재하는 경우, 팝업 상단 고정 영역뿐만 아니라 일반 공지 목록에도 날짜(작성일시) 기준 최신순(내림차순)으로 함께 노출되도록 구현함.
- 백엔드 `GET /community/notices` API에서 전체 공지 항목을 일반 공지 목록에서 필터링해 제외하지 않고 작성일시 내림차순(`created_at.desc()`)으로 반환하도록 변경함.
- 프런트엔드 `NoticeListModal` 팝업의 일반 공지 목록 정렬을 `created_at` 내림차순으로 변경하고 중복 렌더링 키 경고 방지를 처리함.
- 관리자 공지 관리 화면(`CommunityAdminNoticeScreen`, `CommunityAdminScreen`)의 목록 정렬 및 전체 공지 등록 속성을 보완함.
- Python 컴파일 검사(`python -m compileall app`) 및 TypeScript 정적 타입 검사(`npx tsc --noEmit`)를 통과함.
- PR #77로 `main`에 병합 완료함.
- `NoticeListModal`에서 전체 공지 목록과 일반 공지 목록을 상호 배타적인 독립 리스트로 분리함 (중복 제거 및 각각 최신순 정렬).
- PR #79로 `main`에 병합 완료함.
- 관리자 공지 관리 화면(`CommunityAdminNoticeScreen`)의 등록 공지 목록을 공지 팝업과 동일하게 `전체 공지 목록`과 `일반 공지 목록`으로 구분해 출력하도록 개선함.
- PR #80으로 `main`에 병합 완료함.
- `PostOptionsSheet`의 게시물 '공유하기' 실행 시 `Aura+n 게시물 공유됨. [닉네임] 님의 게시물을 확인해보세요.` 메시지와 게시물 임시 URL(`https://auran.app/posts/{id}`)이 포함되어 공유되도록 구현함.
- PR #81로 `main`에 병합 완료함.
- 피드 상세 게시물(`PostDetailModal`, `CommunityPostDetailModal`) 하단 댓글 입력창을 기본 숨김 처리하고, 키보드가 올라오거나 댓글/답글 입력 활성화 시에만 노출되도록 개선함.
- 키보드가 가리는 문제를 방지하기 위해 키보드가 활성화되거나 입력창에 포커스 시 댓글 영역 최하단으로 자동 스크롤(`scrollToEnd`)되는 기능을 추가함.
- PR #82로 `main`에 병합 완료함.
- 관리자가 게시물/댓글 삭제 시 감사 로그(`AuditEvent`)의 `user_id`를 관리자 ID 대신 원 게시물/댓글 작성자(`post.user_id`, `comment.user_id`) ID로 귀속되도록 수정함 (스냅샷에는 관리자 강제 삭제 여부 및 관리자 ID 기록).
- 기존 삭제 감사 로그의 `user_id`를 원 작성자 ID로 보정하는 스타트업 DB 마이그레이션을 추가함.
- PR #83으로 `main`에 병합 완료함.
- 롤백 대비 수정 대상 파일(`AdminScreen.tsx`, `AdminUserPostsModal.tsx`, `adminService.ts`)을 `scratch/backup_proposal1/` 경로에 백업 저장함.
- 제시안 1번 **회원 360° 통합 활동 모달(`AdminUserActivityModal.tsx`)**을 구현하여 회원 클릭 시 프로필, 요약 수치 칩, 개요/보존 이력, 작성 게시물, 작성 댓글, 신고 수신 이력을 탭별로 한눈에 조회하고 관리(계정 정지/해제, 원본/보존 팝업 열기)할 수 있게 적용함.
- PR #84로 `main`에 병합 완료함.
- `AdminUserActivityModal` 내 게시 중 댓글 본문 미노출 버그(백엔드 `display_text` / `content` 교차 참조)를 수정하여 댓글 내용이 100% 명확히 노출되도록 보완함.
- 모달 내 게시물 리스트에서 사진 첨부 시 미디어 썸네일 필수 노출, 사진 없는 게시판 글(제목+내용), 피드 글(사진+캡션), 일자 및 시간(`YYYY. MM. DD. HH:mm`), 게시판 위치 배지(`[피드]`/`[익명게시판]`/`[종합게시판]`) 적용함.
- 신고 탭 항목에 게시물 탭과 동일한 미디어/일시/위치 태그를 표시하고, 관리자가 즉시 조치(`[유지]`, `[숨김]`, `[강제 삭제]`, `[경고 조치]`, `[계정 정지]`)를 선택할 수 있는 드롭다운 액션 메뉴를 추가하고 조치 시 신고자에게 결과 알림이 자동 전송되도록 구현함.
- 중복되던 `활동·보존 이력` 탭 서브 메뉴를 제거하고 `회원 관리` 단일 탭으로 깔끔하게 통합함.
- PR #85로 `main`에 병합 완료함.
- 백엔드 `/admin/users/{user_id}/content` API에서 `PostMedia` 조회 시 `post_id` 타입 미스매치(`str` vs `UUID`)를 해결하고, `AdminUserActivityModal`에 `getMediaUrl` 헬퍼 함수를 추가하여 게시물 미디어(사진 썸네일)가 정상적으로 출력되도록 보완함.
- PR #86으로 `main`에 병합 완료함.
- `AdminScreen.tsx`에 `활동·보존 이력` 탭 및 서브 메뉴를 복원하고, 인라인 펼침 아코디언 내 게시물/댓글 행(`renderActivityContentRow`)에도 미디어 사진 썸네일 필수 표시, 댓글 본문, 일자 및 시간(`YYYY. MM. DD. HH:mm`), 게시판 구분 배지를 완벽히 적용함.
- PR #87로 `main`에 병합 완료함.
- 백엔드 `/admin/users` API에서 사용자별 게시물 수(`posts_count`) 및 댓글 수(`comments_count`)를 직접 계산하여 반환하도록 쿼리를 보완함.
- `AdminScreen.tsx` 회원 관리 탭의 모든 회원 카운트 카드에 `[🖼️ 게시물 N개] [💬 댓글 N개]` 요약 배지를 완벽히 노출되도록 복구함.
- PR #88로 `main`에 병합 완료함.

